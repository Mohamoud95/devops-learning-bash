# Script 56: Create a script to check for updates on the system and install them.

## Purpose
This script automates the process of: **Create a script to check for updates on the system and install them.**

## Script Code (`56-create-a-script-to-check.sh`)
```bash
#!/bin/bash

# Create a script to check for updates on the system and install them.

echo "Checking 
for updates..."sudo apt update && sudo apt upgrade -y
echo "Updates installed."
```

## How it works
This script uses standard Linux utilities to achieve its goal. It processes the required input and executes the logic step-by-step to fulfill the requirements of Bash Problem 56.

## Commands used
- `bash` (script execution)
- Utilities utilized: `echo`


## Key takeaways
- Understanding how to use Bash for automation.
- Proper syntax for conditions, loops, and system commands.
