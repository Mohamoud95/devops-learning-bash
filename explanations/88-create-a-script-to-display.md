# Script 88: Create a script to display the current memory usage in a human-readable format.

## Purpose
This script automates the process of: **Create a script to display the current memory usage in a human-readable format.**

## Script Code (`88-create-a-script-to-display.sh`)
```bash
#!/bin/bash

# Create a script to display the current memory usage in a human-readable format.

free -h
```

## How it works
This script uses standard Linux utilities to achieve its goal. It processes the required input and executes the logic step-by-step to fulfill the requirements of Bash Problem 88.

## Commands used
- `bash` (script execution)
- Utilities utilized: `free`


## Key takeaways
- Understanding how to use Bash for automation.
- Proper syntax for conditions, loops, and system commands.
