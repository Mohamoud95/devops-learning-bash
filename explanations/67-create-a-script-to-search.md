# Script 67: Create a script to search for files by name in a directory.

## Purpose
This script automates the process of: **Create a script to search for files by name in a directory.**

## Script Code (`67-create-a-script-to-search.sh`)
```bash
#!/bin/bash

# Create a script to search for files by name in a directory.

DIRECTORY="/home/user"FILENAME="test.txt"
fi
nd $DIRECTORY -type f -name "$FILENAME"
```

## How it works
This script uses standard Linux utilities to achieve its goal. It processes the required input and executes the logic step-by-step to fulfill the requirements of Bash Problem 67.

## Commands used
- `bash` (script execution)


## Key takeaways
- Understanding how to use Bash for automation.
- Proper syntax for conditions, loops, and system commands.
